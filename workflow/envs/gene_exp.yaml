# find env in scAmpi_single_cell_RNA/workflow/envs/gene_exp.yaml
name: gene_exp
channels:
    - conda-forge
    - bioconda
    - r
    - defaults
dependencies:
    - "r >= 4.0"
    - "r-optparse >= 1.6.6"
    - "r-plyr >= 1.8.6"
    - "r-dplyr >= 1.0.5"
    - "r-ggplot2 >= 3.3.3"
    - "r-purrr >= 0.3.4"
    - "bioconductor-singlecellexperiment >= 1.12.0"
