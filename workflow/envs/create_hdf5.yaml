# find env in scAmpi_single_cell_RNA/workflow/envs/create_hdf5.yaml
name: create_hdf5
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - python >= 3.9
    - numpy >= 1.20.2
    - scipy >= 1.6.2
    - h5py >= 3.1.0
    - configargparse >= 1.4
