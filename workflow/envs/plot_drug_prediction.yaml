# find env in scAmpi_single_cell_RNA/workflow/envs/plot_drug_prediction.yaml
name: plot_drug_prediction
channels:
    - conda-forge
    - bioconda
    - r
    - defaults
dependencies:
    - "r >= 4.0"
    - "r-optparse >= 1.6.6"
    - "bioconductor-singlecellexperiment >= 1.12.0"
    - "r-reshape2 >= 1.4.4"
    - "r-ggplot2 >= 3.3.3"
    - "r-rcolorbrewer >= 1.1_2"
