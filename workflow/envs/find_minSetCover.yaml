# find env in scAmpi_single_cell_RNA/workflow/envs/find_minSetCover.yaml
name: find_minSetCover
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - "python >= 3.9"
    - "configargparse >= 1.4"
