# find env in scAmpi_single_cell_RNA/workflow/envs/query_dgidb.yaml
name: query_dgidb
channels:
    - conda-forge
    - bioconda
    - r
    - defaults
dependencies:
    - "r >= 4.0"
    - "bioconductor-rdgidb >= 1.16.0"
