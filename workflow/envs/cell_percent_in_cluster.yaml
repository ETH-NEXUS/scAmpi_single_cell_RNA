# find env in scAmpi_single_cell_RNA/workflow/envs/cell_percent_in_cluster.yaml
name: cell_percent_in_cluster
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - "python>=3.9"
    - "configargparse>=1.4"
