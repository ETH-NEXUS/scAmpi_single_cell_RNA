# find env in scAmpi_single_cell_RNA/workflow/envs/plot_upsetr.yaml
name: plot_upsetr
channels:
    - conda-forge
    - bioconda
    - r
    - defaults
dependencies:
    - "r >= 4.0"
    - "r-upsetr >= 1.4.0"
    - "r-optparse >= 1.6.6"
    - "r-ggplot2 >= 3.3.3"
