# find env in scAmpi_single_cell_RNA/workflow/envs/get_full_druglist_to_subclones.yaml
name: get_full_druglist_to_subclones
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - "python >= 3.9"
    - "configargparse >= 1.4"
