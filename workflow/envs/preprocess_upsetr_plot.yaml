# find env in scAmpi_single_cell_RNA/workflow/envs/preprocess_upsetr_plot.yaml
name: preprocess_upsetr_plot
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - "python >= 3.9"
    - "configargparse >= 1.4"
