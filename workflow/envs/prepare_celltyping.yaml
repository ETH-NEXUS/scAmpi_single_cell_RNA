# find env in scAmpi_single_cell_RNA/workflow/envs/prepare_celltyping.yaml
name: prepare_celltyping
channels:
    - conda-forge
    - bioconda
    - r
    - defaults
dependencies:
    - r >= 4.0
    - r-optparse >= 1.6.6
    - bioconductor-rhdf5 >= 2.34.0
    - r-reshape2 >= 1.4.4
    - bioconductor-scran >= 1.18.5
    - r-plyr >= 1.8.6
